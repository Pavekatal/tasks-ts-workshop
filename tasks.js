const user = {
    name: "Alice",
    age: 15,
};
const isAdult = (user) => {
    if (user.age >= 18) {
        return true;
    }
    else {
        return false;
    }
};
console.log(isAdult(user));
const movie = {
    id: "01",
    title: "Tenet",
    year: 2020,
    rating: 7,
};
console.log(movie);
export {};
//# sourceMappingURL=tasks.js.map