const user = {
    name: "Alice",
    age: 15,
};
const isAdult = (user) => {
    if (user.age >= 18) {
        return true;
    }
    else {
        return false;
    }
};
console.log(isAdult(user));
export {};
//# sourceMappingURL=tasks.js.map